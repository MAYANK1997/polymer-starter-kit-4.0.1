<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Corporate Actions</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .table-container {
            width: 80%;
            margin: auto;
            margin-top: 20px;
            border: 1px solid #ccc;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }

        .table-header {
            background-color: #003366;
            color: white;
            padding: 10px;
            font-size: 18px;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #0066cc;
            color: white;
        }

        td {
            background-color: #f9f9f9;
        }

        tr:nth-child(even) td {
            background-color: #e6e6e6;
        }
    </style>
</head>
<body>
    <div class="table-container">
        <div class="table-header">
            Corporate Actions Events for the Week 08/19/2024 – 08/23/2024
        </div>
        <table>
            <thead>
                <tr>
                    <th>Day</th>
                    <th>Corporate Action Type</th>
                    <th>Ticker</th>
                </tr>
            </thead>
            <tbody id="table-body">
                <!-- Data from the API will be inserted here -->
            </tbody>
        </table>
    </div>

    <script>
        // Function to fetch data from a backend API
        async function fetchTableData() {
            try {
                // Fetch data from an API (replace this with your actual API URL)
                const response = await fetch('https://api.example.com/corporate-actions');
                const data = await response.json();

                // Select the table body
                const tableBody = document.getElementById('table-body');

                // Clear any existing rows
                tableBody.innerHTML = '';

                // Iterate over the data and create table rows
                data.forEach(item => {
                    const row = `<tr>
                                    <td>${item.day}</td>
                                    <td>${item.actionType}</td>
                                    <td>${item.ticker}</td>
                                </tr>`;
                    tableBody.innerHTML += row;
                });
            } catch (error) {
                console.error('Error fetching table data:', error);
            }
        }

        // Fetch the table data when the page loads
        window.onload = fetchTableData;
    </script>
</body>
</html>
